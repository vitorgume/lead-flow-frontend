export default interface Cliente {
    id: string;
    nome: string;
    telefone: string;
    regiao: string;
    segmento: string;
    inativo: boolean;
    descricaoMaterial: string;
    canal: string;
}